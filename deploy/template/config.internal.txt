{
    "delay": 0,
    "port": {{ port }},
    "thread": 0,
    "database": 
    [
        {% for db in database -%}
        {
            "ip": "{{ db.ip }}",
            "port": {{ db.port }},
            "name": "{{ db.name}}",
            "uid": "{{ db.uid }}",
            "pwd": "{{ db.pwd }}"
        }
        {%- if not loop.last %}, 
        {% endif %}
        {%- endfor %}
    ],
    "table": {
        "host": "table/host.json"
    },
    "hosts": {{ game }}
}
