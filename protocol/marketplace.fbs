namespace fb.protocol.marketplace;

enum ListingState : ubyte {
    ACTIVE = 0,
    SOLD = 1,
    CANCELLED = 2,
    EXPIRED = 3
}

table Item {
    owner: uint;
    model: uint;
    count: ushort;
    durability: uint?;
    custom_name: string?;
}

table Listing {
    id: string;
    seller_id: uint;
    buyer_id: uint;
    item: Item;
    price: uint;
    transaction_fee: uint;
    state: fb.protocol.marketplace.ListingState;
    expire_date: string;
    created_date: string;
}

table SearchResult {
    listings: [Listing];
    total_count: uint;
    page: uint;
}

