// Auto-generated by tools/codegen/integration_protocol/generate.py
// DO NOT EDIT

#include <fb/bot/integration/lua_integration_protocol.h>
#include <fb/bot/integration/protocol_registry.h>
#include <fb/game/protocol.h>
#include <fb/model/model.h>
#include <fb/lua.h>

#include <memory>
#include <string>

namespace fb::bot::integration {

namespace game_reqs = fb::protocol::game::request;
namespace game_resp = fb::protocol::game::response;

constexpr auto BOT_CLIENT_VERSION = fb::protocol::CLIENT_VERSION::v550;

namespace detail {

void marshal_lua_game_resp__action(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::action&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("value");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.value));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("value_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.value));
    lua->settable(-3);
    lua->pushstring("duration");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.duration));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("duration_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.duration));
    lua->settable(-3);
    lua->pushstring("sound");
    lua->pushinteger(resp.sound);
    lua->settable(-3);
}

void marshal_lua_game_resp__ad(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::ad&>(header);
    lua->new_table();
    lua->pushstring("width");
    lua->pushinteger(resp.width);
    lua->settable(-3);
    lua->pushstring("height");
    lua->pushinteger(resp.height);
    lua->settable(-3);
    lua->pushstring("url");
    lua->pushstring(resp.url);
    lua->settable(-3);
    lua->pushstring("time");
    lua->pushinteger(resp.time);
    lua->settable(-3);
}

void marshal_lua_game_resp__bright(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bright&>(header);
    lua->new_table();
    lua->pushstring("value");
    lua->pushinteger(resp.value);
    lua->settable(-3);
}

void marshal_lua_game_resp__browser(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::browser<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__bulletin_article(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bulletin_article&>(header);
    lua->new_table();
    lua->pushstring("id");
    lua->pushinteger(resp.id);
    lua->settable(-3);
    lua->pushstring("uname");
    lua->pushstring(resp.uname);
    lua->settable(-3);
    lua->pushstring("month");
    lua->pushinteger(resp.month);
    lua->settable(-3);
    lua->pushstring("day");
    lua->pushinteger(resp.day);
    lua->settable(-3);
    lua->pushstring("title");
    lua->pushstring(resp.title);
    lua->settable(-3);
    lua->pushstring("contents");
    lua->pushstring(resp.contents);
    lua->settable(-3);
}

void marshal_lua_game_resp__bulletin_articles(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bulletin_articles&>(header);
    lua->new_table();
    lua->pushstring("bulletin_id");
    lua->pushinteger(resp.bulletin_id);
    lua->settable(-3);
    lua->pushstring("bulletin_name");
    lua->pushstring(resp.bulletin_name);
    lua->settable(-3);
}

void marshal_lua_game_resp__bulletin_mail(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bulletin_mail&>(header);
    lua->new_table();
    lua->pushstring("id");
    lua->pushinteger(resp.id);
    lua->settable(-3);
    lua->pushstring("sender");
    lua->pushstring(resp.sender);
    lua->settable(-3);
    lua->pushstring("month");
    lua->pushinteger(resp.month);
    lua->settable(-3);
    lua->pushstring("day");
    lua->pushinteger(resp.day);
    lua->settable(-3);
    lua->pushstring("title");
    lua->pushstring(resp.title);
    lua->settable(-3);
    lua->pushstring("contents");
    lua->pushstring(resp.contents);
    lua->settable(-3);
}

void marshal_lua_game_resp__bulletin_mails(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bulletin_mails&>(header);
    lua->new_table();
    lua->pushstring("mail_name");
    lua->pushstring(resp.mail_name);
    lua->settable(-3);
}

void marshal_lua_game_resp__bulletin_message(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bulletin_message&>(header);
    lua->new_table();
    lua->pushstring("text");
    lua->pushstring(resp.text);
    lua->settable(-3);
    lua->pushstring("success");
    lua->pushboolean(resp.success);
    lua->settable(-3);
    lua->pushstring("action");
    lua->pushinteger(resp.action);
    lua->settable(-3);
}

void marshal_lua_game_resp__bulletin_sections(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::bulletin_sections&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__c2s_relay(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::c2s_relay&>(header);
    lua->new_table();
    lua->pushstring("payload");
    lua->pushstring(resp.payload);
    lua->settable(-3);
}

void marshal_lua_game_resp__chat(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::chat&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("text");
    lua->pushstring(resp.text);
    lua->settable(-3);
    lua->pushstring("type");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.type));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("type_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.type));
    lua->settable(-3);
}

void marshal_lua_game_resp__collection(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::collection<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("slot");
    lua->pushinteger(resp.slot);
    lua->settable(-3);
    lua->pushstring("flag");
    lua->pushinteger(resp.flag);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("button_prev");
    lua->pushboolean(resp.button_prev);
    lua->settable(-3);
    lua->pushstring("button_next");
    lua->pushboolean(resp.button_next);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_0x30_10(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_0x30_10&>(header);
    lua->new_table();
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("button_prev");
    lua->pushboolean(resp.button_prev);
    lua->settable(-3);
    lua->pushstring("button_next");
    lua->pushboolean(resp.button_next);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_dual_field(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_dual_field&>(header);
    lua->new_table();
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("pursuit");
    lua->pushinteger(resp.pursuit);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_email(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_email&>(header);
    lua->new_table();
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("str1");
    lua->pushstring(resp.str1);
    lua->settable(-3);
    lua->pushstring("str2");
    lua->pushstring(resp.str2);
    lua->settable(-3);
    lua->pushstring("button_prev");
    lua->pushboolean(resp.button_prev);
    lua->settable(-3);
    lua->pushstring("button_next");
    lua->pushboolean(resp.button_next);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_input(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_input&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("pursuit");
    lua->pushinteger(resp.pursuit);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_input_ext(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_input_ext&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("top");
    lua->pushstring(resp.top);
    lua->settable(-3);
    lua->pushstring("bottom");
    lua->pushstring(resp.bottom);
    lua->settable(-3);
    lua->pushstring("maxlen");
    lua->pushinteger(resp.maxlen);
    lua->settable(-3);
    lua->pushstring("button_prev");
    lua->pushboolean(resp.button_prev);
    lua->settable(-3);
    lua->pushstring("password");
    lua->pushboolean(resp.password);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_item(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_item&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("pursuit");
    lua->pushinteger(resp.pursuit);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_list(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_list&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("button_prev");
    lua->pushboolean(resp.button_prev);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_menu(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_menu&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_pursuit(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_pursuit&>(header);
    lua->new_table();
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("pursuit");
    lua->pushinteger(resp.pursuit);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_slot(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_slot&>(header);
    lua->new_table();
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__dialog_spell(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::dialog_spell&>(header);
    lua->new_table();
    lua->pushstring("type_echo");
    lua->pushinteger(resp.type_echo);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
    lua->pushstring("pursuit");
    lua->pushinteger(resp.pursuit);
    lua->settable(-3);
}

void marshal_lua_game_resp__die(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::die&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__direction(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::direction&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("value");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.value));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("value_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.value));
    lua->settable(-3);
}

void marshal_lua_game_resp__effect(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::effect<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("value");
    lua->pushinteger(resp.value);
    lua->settable(-3);
}

void marshal_lua_game_resp__external_info(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::external_info<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("title");
    lua->pushstring(resp.title);
    lua->settable(-3);
    lua->pushstring("clan_name");
    lua->pushstring(resp.clan_name);
    lua->settable(-3);
    lua->pushstring("clan_title");
    lua->pushstring(resp.clan_title);
    lua->settable(-3);
    lua->pushstring("class_name");
    lua->pushstring(resp.class_name);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
    lua->pushstring("disguised");
    lua->pushinteger(resp.disguised);
    lua->settable(-3);
    lua->pushstring("gender");
    lua->pushinteger(resp.gender);
    lua->settable(-3);
    lua->pushstring("state");
    lua->pushinteger(resp.state);
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("armor_dress");
    lua->pushinteger(resp.armor_dress);
    lua->settable(-3);
    lua->pushstring("armor_color");
    lua->pushinteger(resp.armor_color);
    lua->settable(-3);
    lua->pushstring("weapon_dress");
    lua->pushinteger(resp.weapon_dress);
    lua->settable(-3);
    lua->pushstring("weapon_color");
    lua->pushinteger(resp.weapon_color);
    lua->settable(-3);
    lua->pushstring("shield_dress");
    lua->pushinteger(resp.shield_dress);
    lua->settable(-3);
    lua->pushstring("shield_color");
    lua->pushinteger(resp.shield_color);
    lua->settable(-3);
    lua->pushstring("equipment_text");
    lua->pushstring(resp.equipment_text);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("group_option");
    lua->pushinteger(resp.group_option);
    lua->settable(-3);
    lua->pushstring("trade_option");
    lua->pushinteger(resp.trade_option);
    lua->settable(-3);
    lua->pushstring("reputation");
    lua->pushinteger(resp.reputation);
    lua->settable(-3);
}

void marshal_lua_game_resp__freeze(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::freeze&>(header);
    lua->new_table();
    lua->pushstring("enable");
    lua->pushboolean(resp.enable);
    lua->settable(-3);
}

void marshal_lua_game_resp__friends_sync(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::friends_sync&>(header);
    lua->new_table();
    lua->pushstring("enabled");
    lua->pushinteger(resp.enabled);
    lua->settable(-3);
}

void marshal_lua_game_resp__group_portrait(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::group_portrait<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("subtype");
    lua->pushinteger(resp.subtype);
    lua->settable(-3);
    lua->pushstring("count");
    lua->pushinteger(resp.count);
    lua->settable(-3);
}

void marshal_lua_game_resp__hide(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::hide&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
}

void marshal_lua_game_resp__holyday_screen(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::holyday_screen&>(header);
    lua->new_table();
    lua->pushstring("screen");
    lua->pushinteger(resp.screen);
    lua->settable(-3);
    lua->pushstring("hair");
    lua->pushinteger(resp.hair);
    lua->settable(-3);
    lua->pushstring("direction");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.direction));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("direction_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.direction));
    lua->settable(-3);
}

void marshal_lua_game_resp__id(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::id&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("direction");
    lua->pushinteger(resp.direction);
    lua->settable(-3);
    lua->pushstring("cls");
    lua->pushinteger(resp.cls);
    lua->settable(-3);
}

void marshal_lua_game_resp__init(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::init&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__internal_info(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::internal_info<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("phydef");
    lua->pushinteger(resp.phydef);
    lua->settable(-3);
    lua->pushstring("dam");
    lua->pushinteger(resp.dam);
    lua->settable(-3);
    lua->pushstring("hit");
    lua->pushinteger(resp.hit);
    lua->settable(-3);
    lua->pushstring("clan_name");
    lua->pushstring(resp.clan_name);
    lua->settable(-3);
    lua->pushstring("clan_title");
    lua->pushstring(resp.clan_title);
    lua->settable(-3);
    lua->pushstring("title");
    lua->pushstring(resp.title);
    lua->settable(-3);
    lua->pushstring("group_info");
    lua->pushstring(resp.group_info);
    lua->settable(-3);
    lua->pushstring("group_option");
    lua->pushinteger(resp.group_option);
    lua->settable(-3);
    lua->pushstring("remained_exp");
    lua->pushinteger(resp.remained_exp);
    lua->settable(-3);
    lua->pushstring("class_name");
    lua->pushstring(resp.class_name);
    lua->settable(-3);
    lua->pushstring("trade_option");
    lua->pushinteger(resp.trade_option);
    lua->settable(-3);
    lua->pushstring("pk_protect_option");
    lua->pushinteger(resp.pk_protect_option);
    lua->settable(-3);
}

void marshal_lua_game_resp__item_remove(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_remove&>(header);
    lua->new_table();
    lua->pushstring("index");
    lua->pushinteger(resp.index);
    lua->settable(-3);
    lua->pushstring("count");
    lua->pushinteger(resp.count);
    lua->settable(-3);
}

void marshal_lua_game_resp__item_throw_confirm(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_throw_confirm&>(header);
    lua->new_table();
    lua->pushstring("inventory_slot");
    lua->pushinteger(resp.inventory_slot);
    lua->settable(-3);
}

void marshal_lua_game_resp__item_throws(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_throws&>(header);
    lua->new_table();
    lua->pushstring("ch_oid");
    lua->pushinteger(resp.ch_oid);
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("item_oid");
    lua->pushinteger(resp.item_oid);
    lua->settable(-3);
    lua->pushstring("from_x");
    lua->pushinteger(resp.from_x);
    lua->settable(-3);
    lua->pushstring("from_y");
    lua->pushinteger(resp.from_y);
    lua->settable(-3);
    lua->pushstring("to_x");
    lua->pushinteger(resp.to_x);
    lua->settable(-3);
    lua->pushstring("to_y");
    lua->pushinteger(resp.to_y);
    lua->settable(-3);
}

void marshal_lua_game_resp__item_tip(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_tip<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("position");
    lua->pushinteger(resp.position);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
}

void marshal_lua_game_resp__item_unequip(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_unequip<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("parts");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.parts));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("parts_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.parts));
    lua->settable(-3);
}

void marshal_lua_game_resp__item_update(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_update<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("index");
    lua->pushinteger(resp.index);
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
    lua->pushstring("count");
    lua->pushinteger(resp.count);
    lua->settable(-3);
}

void marshal_lua_game_resp__item_update_slot(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::item_update_slot<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("parts");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.parts));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("parts_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.parts));
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
    lua->pushstring("unknown_name_b");
    lua->pushstring(resp.unknown_name_b);
    lua->settable(-3);
    lua->pushstring("unknown_u32");
    lua->pushinteger(resp.unknown_u32);
    lua->settable(-3);
}

void marshal_lua_game_resp__map_bgm(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::map_bgm&>(header);
    lua->new_table();
    lua->pushstring("bgm");
    lua->pushinteger(resp.bgm);
    lua->settable(-3);
    lua->pushstring("volume");
    lua->pushinteger(resp.volume);
    lua->settable(-3);
}

void marshal_lua_game_resp__map_bgm_stop(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::map_bgm_stop&>(header);
    lua->new_table();
    lua->pushstring("bgm_id");
    lua->pushinteger(resp.bgm_id);
    lua->settable(-3);
}

void marshal_lua_game_resp__map_config(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::map_config<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("id");
    lua->pushinteger(resp.id);
    lua->settable(-3);
    lua->pushstring("building");
    lua->pushboolean(resp.building);
    lua->settable(-3);
    lua->pushstring("flags");
    lua->pushinteger(resp.flags);
    lua->settable(-3);
    lua->pushstring("extra");
    lua->pushinteger(resp.extra);
    lua->settable(-3);
    lua->pushstring("light");
    lua->pushinteger(resp.light);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
}

void marshal_lua_game_resp__map_update(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::map_update&>(header);
    lua->new_table();
    lua->pushstring("effect");
    lua->pushinteger(resp.effect);
    lua->settable(-3);
    lua->pushstring("begin_x");
    lua->pushinteger(resp.begin_x);
    lua->settable(-3);
    lua->pushstring("begin_y");
    lua->pushinteger(resp.begin_y);
    lua->settable(-3);
    lua->pushstring("width");
    lua->pushinteger(resp.width);
    lua->settable(-3);
    lua->pushstring("height");
    lua->pushinteger(resp.height);
    lua->settable(-3);
}

void marshal_lua_game_resp__map_worlds(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::map_worlds&>(header);
    lua->new_table();
    lua->pushstring("key");
    lua->pushstring(resp.key);
    lua->settable(-3);
    lua->pushstring("world_count");
    lua->pushinteger(resp.world_count);
    lua->settable(-3);
    lua->pushstring("index");
    lua->pushinteger(resp.index);
    lua->settable(-3);
}

void marshal_lua_game_resp__message(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::message&>(header);
    lua->new_table();
    lua->pushstring("text");
    lua->pushstring(resp.text);
    lua->settable(-3);
    lua->pushstring("type");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.type));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("type_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.type));
    lua->settable(-3);
}

void marshal_lua_game_resp__move(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::move&>(header);
    lua->new_table();
    lua->pushstring("id");
    lua->pushinteger(resp.id);
    lua->settable(-3);
    lua->pushstring("position");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.position.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.position.y);
    lua->settable(-3);
    lua->settable(-3);
    lua->pushstring("direction");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.direction));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("direction_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.direction));
    lua->settable(-3);
}

void marshal_lua_game_resp__move_confirm(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::move_confirm<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("direction");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.direction));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("direction_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.direction));
    lua->settable(-3);
    lua->pushstring("position");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.position.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.position.y);
    lua->settable(-3);
    lua->settable(-3);
    lua->pushstring("viewport");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.viewport.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.viewport.y);
    lua->settable(-3);
    lua->settable(-3);
    lua->pushstring("walk_queue_slot");
    lua->pushinteger(resp.walk_queue_slot);
    lua->settable(-3);
}

void marshal_lua_game_resp__move_confirm_noscroll(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::move_confirm_noscroll&>(header);
    lua->new_table();
    lua->pushstring("direction");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.direction));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("direction_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.direction));
    lua->settable(-3);
    lua->pushstring("position");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.position.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.position.y);
    lua->settable(-3);
    lua->settable(-3);
    lua->pushstring("viewport");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.viewport.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.viewport.y);
    lua->settable(-3);
    lua->settable(-3);
    lua->pushstring("walk_queue_slot");
    lua->pushinteger(resp.walk_queue_slot);
    lua->settable(-3);
}

void marshal_lua_game_resp__notice(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::notice<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("flag");
    lua->pushinteger(resp.flag);
    lua->settable(-3);
    lua->pushstring("text");
    lua->pushstring(resp.text);
    lua->settable(-3);
}

void marshal_lua_game_resp__option(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::option<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("weather_effect");
    lua->pushboolean(resp.weather_effect);
    lua->settable(-3);
    lua->pushstring("magic_effect");
    lua->pushboolean(resp.magic_effect);
    lua->settable(-3);
    lua->pushstring("news");
    lua->pushboolean(resp.news);
    lua->settable(-3);
    lua->pushstring("fast_move");
    lua->pushboolean(resp.fast_move);
    lua->settable(-3);
    lua->pushstring("effect_sound");
    lua->pushboolean(resp.effect_sound);
    lua->settable(-3);
    lua->pushstring("visible_helmet");
    lua->pushboolean(resp.visible_helmet);
    lua->settable(-3);
}

void marshal_lua_game_resp__ping(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::ping&>(header);
    lua->new_table();
    lua->pushstring("value");
    lua->pushinteger(resp.value);
    lua->settable(-3);
}

void marshal_lua_game_resp__popup_input(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::popup_input&>(header);
    lua->new_table();
    lua->pushstring("param0");
    lua->pushinteger(resp.param0);
    lua->settable(-3);
    lua->pushstring("param1");
    lua->pushinteger(resp.param1);
    lua->settable(-3);
    lua->pushstring("param2");
    lua->pushinteger(resp.param2);
    lua->settable(-3);
    lua->pushstring("param3");
    lua->pushinteger(resp.param3);
    lua->settable(-3);
    lua->pushstring("text");
    lua->pushstring(resp.text);
    lua->settable(-3);
}

void marshal_lua_game_resp__popup_message(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::popup_message&>(header);
    lua->new_table();
    lua->pushstring("param0");
    lua->pushinteger(resp.param0);
    lua->settable(-3);
    lua->pushstring("param1");
    lua->pushinteger(resp.param1);
    lua->settable(-3);
    lua->pushstring("param2");
    lua->pushinteger(resp.param2);
    lua->settable(-3);
    lua->pushstring("param3");
    lua->pushinteger(resp.param3);
    lua->settable(-3);
    lua->pushstring("text");
    lua->pushstring(resp.text);
    lua->settable(-3);
}

void marshal_lua_game_resp__position(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::position&>(header);
    lua->new_table();
    lua->pushstring("abs");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.abs.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.abs.y);
    lua->settable(-3);
    lua->settable(-3);
    lua->pushstring("rel");
    lua->new_table();
    lua->pushinteger(1);
    lua->pushinteger(resp.rel.x);
    lua->settable(-3);
    lua->pushinteger(2);
    lua->pushinteger(resp.rel.y);
    lua->settable(-3);
    lua->settable(-3);
}

void marshal_lua_game_resp__save(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::save&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__screen_refresh_complete(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::screen_refresh_complete&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__show(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::show<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("x");
    lua->pushinteger(resp.x);
    lua->settable(-3);
    lua->pushstring("y");
    lua->pushinteger(resp.y);
    lua->settable(-3);
    lua->pushstring("direction");
    lua->pushinteger(resp.direction);
    lua->settable(-3);
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("disguised");
    lua->pushinteger(resp.disguised);
    lua->settable(-3);
    lua->pushstring("state");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.state));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("state_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.state));
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("armor_dress");
    lua->pushinteger(resp.armor_dress);
    lua->settable(-3);
    lua->pushstring("armor_color");
    lua->pushinteger(resp.armor_color);
    lua->settable(-3);
    lua->pushstring("weapon_dress");
    lua->pushinteger(resp.weapon_dress);
    lua->settable(-3);
    lua->pushstring("weapon_color");
    lua->pushinteger(resp.weapon_color);
    lua->settable(-3);
    lua->pushstring("shield_dress");
    lua->pushinteger(resp.shield_dress);
    lua->settable(-3);
    lua->pushstring("shield_color");
    lua->pushinteger(resp.shield_color);
    lua->settable(-3);
    lua->pushstring("head_marker");
    lua->pushinteger(resp.head_marker);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
}

void marshal_lua_game_resp__sound(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::sound&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("volume");
    lua->pushinteger(resp.volume);
    lua->settable(-3);
}

void marshal_lua_game_resp__spell_buff(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::spell_buff&>(header);
    lua->new_table();
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
}

void marshal_lua_game_resp__spell_delay(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::spell_delay&>(header);
    lua->new_table();
    lua->pushstring("spell_name");
    lua->pushstring(resp.spell_name);
    lua->settable(-3);
    lua->pushstring("delay");
    lua->pushinteger(resp.delay);
    lua->settable(-3);
}

void marshal_lua_game_resp__spell_remove(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::spell_remove&>(header);
    lua->new_table();
    lua->pushstring("index");
    lua->pushinteger(resp.index);
    lua->settable(-3);
}

void marshal_lua_game_resp__spell_unbuff(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::spell_unbuff&>(header);
    lua->new_table();
    lua->pushstring("buff_name");
    lua->pushstring(resp.buff_name);
    lua->settable(-3);
}

void marshal_lua_game_resp__spell_update(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::spell_update<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("index");
    lua->pushinteger(resp.index);
    lua->settable(-3);
    lua->pushstring("type");
    lua->pushinteger(resp.type);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
}

void marshal_lua_game_resp__time(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::time&>(header);
    lua->new_table();
    lua->pushstring("hours");
    lua->pushinteger(resp.hours);
    lua->settable(-3);
    lua->pushstring("minutes");
    lua->pushinteger(resp.minutes);
    lua->settable(-3);
}

void marshal_lua_game_resp__timer(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::timer&>(header);
    lua->new_table();
    lua->pushstring("time");
    lua->pushinteger(resp.time);
    lua->settable(-3);
}

void marshal_lua_game_resp__trade_bundle(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::trade_bundle&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__trade_close(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::trade_close&>(header);
    lua->new_table();
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
}

void marshal_lua_game_resp__trade_dialog(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::trade_dialog<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
    lua->pushstring("reputation");
    lua->pushinteger(resp.reputation);
    lua->settable(-3);
}

void marshal_lua_game_resp__trade_lock(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::trade_lock&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__trade_money(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::trade_money&>(header);
    lua->new_table();
    lua->pushstring("money");
    lua->pushinteger(resp.money);
    lua->settable(-3);
    lua->pushstring("mine");
    lua->pushboolean(resp.mine);
    lua->settable(-3);
}

void marshal_lua_game_resp__trade_upload(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::trade_upload&>(header);
    lua->new_table();
    lua->pushstring("index");
    lua->pushinteger(resp.index);
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
    lua->pushstring("mine");
    lua->pushboolean(resp.mine);
    lua->settable(-3);
}

void marshal_lua_game_resp__ui_screen(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::ui_screen&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__unknown_4f(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::unknown_4f<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__update(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::update<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("object_count");
    lua->pushinteger(resp.object_count);
    lua->settable(-3);
    lua->pushstring("objects_data");
    lua->new_table();
    for (size_t i = 0; i < resp.objects_data.size(); ++i)
    {
        const auto& object = resp.objects_data[i];
        lua->pushinteger(static_cast<lua_Integer>(i + 1));
        lua->new_table();
        lua->pushstring("x");
        lua->pushinteger(object.x);
        lua->settable(-3);
        lua->pushstring("y");
        lua->pushinteger(object.y);
        lua->settable(-3);
        lua->pushstring("oid");
        lua->pushinteger(object.oid);
        lua->settable(-3);
        lua->pushstring("look");
        lua->pushinteger(object.look);
        lua->settable(-3);
        lua->pushstring("color");
        lua->pushinteger(object.color);
        lua->settable(-3);
        lua->pushstring("direction");
        lua->pushinteger(object.direction);
        lua->settable(-3);
        lua->settable(-3);
    }
    lua->settable(-3);
}

void marshal_lua_game_resp__update_cc(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::update_cc&>(header);
    lua->new_table();
    const auto cc = static_cast<uint32_t>(resp.cc);
    lua->pushstring("cc");
    lua->pushinteger(cc);
    lua->settable(-3);
    lua->pushstring("direction");
    lua->pushboolean((cc & static_cast<uint32_t>(fb::model::enum_value::CROWD_CONTROL::DIRECTION)) != 0);
    lua->settable(-3);
    lua->pushstring("sight");
    lua->pushboolean((cc & static_cast<uint32_t>(fb::model::enum_value::CROWD_CONTROL::SIGHT)) != 0);
    lua->settable(-3);
    lua->pushstring("hear");
    lua->pushboolean((cc & static_cast<uint32_t>(fb::model::enum_value::CROWD_CONTROL::HEAR)) != 0);
    lua->settable(-3);
    lua->pushstring("chat");
    lua->pushboolean((cc & static_cast<uint32_t>(fb::model::enum_value::CROWD_CONTROL::CHAT)) != 0);
    lua->settable(-3);
}

void marshal_lua_game_resp__update_external(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::update_external<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("disguised");
    lua->pushinteger(resp.disguised);
    lua->settable(-3);
    lua->pushstring("state");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.state));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("state_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.state));
    lua->settable(-3);
    lua->pushstring("look");
    lua->pushinteger(resp.look);
    lua->settable(-3);
    lua->pushstring("color");
    lua->pushinteger(resp.color);
    lua->settable(-3);
    lua->pushstring("armor_dress");
    lua->pushinteger(resp.armor_dress);
    lua->settable(-3);
    lua->pushstring("armor_color");
    lua->pushinteger(resp.armor_color);
    lua->settable(-3);
    lua->pushstring("weapon_dress");
    lua->pushinteger(resp.weapon_dress);
    lua->settable(-3);
    lua->pushstring("weapon_color");
    lua->pushinteger(resp.weapon_color);
    lua->settable(-3);
    lua->pushstring("shield_dress");
    lua->pushinteger(resp.shield_dress);
    lua->settable(-3);
    lua->pushstring("shield_color");
    lua->pushinteger(resp.shield_color);
    lua->settable(-3);
    lua->pushstring("head_marker");
    lua->pushinteger(resp.head_marker);
    lua->settable(-3);
    lua->pushstring("name");
    lua->pushstring(resp.name);
    lua->settable(-3);
}

void marshal_lua_game_resp__update_hp(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::update_hp&>(header);
    lua->new_table();
    lua->pushstring("oid");
    lua->pushinteger(resp.oid);
    lua->settable(-3);
    lua->pushstring("damage");
    lua->pushinteger(resp.damage);
    lua->settable(-3);
    lua->pushstring("critical");
    lua->pushinteger(resp.critical);
    lua->settable(-3);
    lua->pushstring("percent");
    lua->pushinteger(resp.percent);
    lua->settable(-3);
}

void marshal_lua_game_resp__update_internal(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::update_internal<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("level");
    try
    {
        lua->pushstring(fb::model::enum_value::enum_tostring(resp.level));
    }
    catch (...)
    {
        lua->pushnil();
    }
    lua->settable(-3);
    lua->pushstring("level_id");
    lua->pushinteger(static_cast<lua_Integer>(resp.level));
    lua->settable(-3);
    lua->pushstring("ch_follow_camera");
    lua->pushboolean(resp.ch_follow_camera);
    lua->settable(-3);
    lua->pushstring("ch_lock_walk_speed");
    lua->pushboolean(resp.ch_lock_walk_speed);
    lua->settable(-3);
    lua->pushstring("ch_gm");
    lua->pushboolean(resp.ch_gm);
    lua->settable(-3);
    lua->pushstring("ch_nation");
    lua->pushinteger(resp.ch_nation);
    lua->settable(-3);
    lua->pushstring("ch_divine_beast");
    lua->pushinteger(resp.ch_divine_beast);
    lua->settable(-3);
    lua->pushstring("unknown_based_5");
    lua->pushinteger(resp.unknown_based_5);
    lua->settable(-3);
    lua->pushstring("ch_level");
    lua->pushinteger(resp.ch_level);
    lua->settable(-3);
    lua->pushstring("ch_base_hp");
    lua->pushinteger(resp.ch_base_hp);
    lua->settable(-3);
    lua->pushstring("ch_base_mp");
    lua->pushinteger(resp.ch_base_mp);
    lua->settable(-3);
    lua->pushstring("ch_strength");
    lua->pushinteger(resp.ch_strength);
    lua->settable(-3);
    lua->pushstring("ch_intelligence");
    lua->pushinteger(resp.ch_intelligence);
    lua->settable(-3);
    lua->pushstring("ch_dexterity");
    lua->pushinteger(resp.ch_dexterity);
    lua->settable(-3);
    lua->pushstring("reputation");
    lua->pushinteger(resp.reputation);
    lua->settable(-3);
    lua->pushstring("evaluation");
    lua->pushinteger(resp.evaluation);
    lua->settable(-3);
    lua->pushstring("ch_hp");
    lua->pushinteger(resp.ch_hp);
    lua->settable(-3);
    lua->pushstring("ch_mp");
    lua->pushinteger(resp.ch_mp);
    lua->settable(-3);
    lua->pushstring("ch_exp");
    lua->pushinteger(resp.ch_exp);
    lua->settable(-3);
    lua->pushstring("ch_money");
    lua->pushinteger(resp.ch_money);
    lua->settable(-3);
    lua->pushstring("ch_crowd_control");
    lua->pushinteger(resp.ch_crowd_control);
    lua->settable(-3);
    lua->pushstring("ch_mail");
    lua->pushinteger(resp.ch_mail);
    lua->settable(-3);
    lua->pushstring("ch_fast_move");
    lua->pushinteger(resp.ch_fast_move);
    lua->settable(-3);
    lua->pushstring("ch_speed");
    lua->pushinteger(resp.ch_speed);
    lua->settable(-3);
    lua->pushstring("unknown_exp_pad");
    lua->pushinteger(resp.unknown_exp_pad);
    lua->settable(-3);
    lua->pushstring("option_bits");
    lua->pushinteger(resp.option_bits);
    lua->settable(-3);
}

void marshal_lua_game_resp__user_info(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::user_info&>(header);
    lua->new_table();
    lua->pushstring("type");
    lua->pushinteger(resp.type);
    lua->settable(-3);
}

void marshal_lua_game_resp__user_list(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::user_list&>(header);
    lua->new_table();
    lua->pushstring("users");
    lua->new_table();
    for (size_t i = 0; i < resp.users.size(); ++i)
    {
        const auto& user = resp.users[i];
        lua->pushinteger(static_cast<lua_Integer>(i + 1));
        lua->new_table();
        lua->pushstring("nation");
        lua->pushinteger(user.nation);
        lua->settable(-3);
        lua->pushstring("cls");
        lua->pushinteger(user.cls);
        lua->settable(-3);
        lua->pushstring("promotion");
        lua->pushinteger(user.promotion);
        lua->settable(-3);
        lua->pushstring("level");
        lua->pushinteger(user.level);
        lua->settable(-3);
        lua->pushstring("color");
        lua->pushinteger(user.color);
        lua->settable(-3);
        lua->pushstring("name");
        lua->pushstring(user.name);
        lua->settable(-3);
        lua->settable(-3);
    }
    lua->settable(-3);
}

void marshal_lua_game_resp__weather(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::weather&>(header);
    lua->new_table();
}

void marshal_lua_game_resp__web(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::web<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
    lua->pushstring("type");
    lua->pushinteger(resp.type);
    lua->settable(-3);
    lua->pushstring("address");
    lua->pushstring(resp.address);
    lua->settable(-3);
    lua->pushstring("message");
    lua->pushstring(resp.message);
    lua->settable(-3);
}

void marshal_lua_game_resp__web_map(lua_State* L, const fb::protocol::header& header)
{
    auto* lua = fb::lua::get(L);
    if (lua == nullptr)
        return;

    const auto& resp = static_cast<const game_resp::web_map<BOT_CLIENT_VERSION>&>(header);
    lua->new_table();
}

} // namespace detail

} // namespace fb::bot::integration

namespace fb::bot::integration::lua_protocol {

int lua_builder_game_reqs__attack(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::attack<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__browser(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto type = static_cast<uint8_t>(lua->tointeger(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::browser<BOT_CLIENT_VERSION>>(type));
    return 1;
}

int lua_builder_game_reqs__bulletin(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto action   = fb::model::enum_value::enum_parse<fb::model::enum_value::BULLETIN_ACTION>(lua->tostring(1));
    const auto section  = static_cast<uint16_t>(lua->tointeger(2));
    const auto article  = static_cast<uint16_t>(lua->tointeger(3));
    const auto offset   = static_cast<uint16_t>(lua->tointeger(4));
    const auto title    = lua->tostring(5);
    const auto contents = lua->tostring(6);
    const auto user     = lua->tostring(7);
    lua_protocol::push_request(L,
                               std::make_shared<game_reqs::bulletin<BOT_CLIENT_VERSION>>(action,
                                                                                         section,
                                                                                         article,
                                                                                         offset,
                                                                                         title,
                                                                                         contents,
                                                                                         user));
    return 1;
}

int lua_builder_game_reqs__chat(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto shout   = lua->toboolean(1);
    const auto message = lua->tostring(2);
    lua_protocol::push_request(L, std::make_shared<game_reqs::chat<BOT_CLIENT_VERSION>>(shout, message));
    return 1;
}

int lua_builder_game_reqs__click(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto oid  = static_cast<uint32_t>(lua->tointeger(1));
    const auto flag = static_cast<uint8_t>(lua->tointeger(2));
    lua_protocol::push_request(L, std::make_shared<game_reqs::click<BOT_CLIENT_VERSION>>(oid, flag));
    return 1;
}

int lua_builder_game_reqs__collection(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto slot = static_cast<uint8_t>(lua->tointeger(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::collection<BOT_CLIENT_VERSION>>(slot));
    return 1;
}

int lua_builder_game_reqs__direction(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto value = fb::model::enum_value::enum_parse<fb::model::enum_value::DIRECTION>(lua->tostring(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::direction<BOT_CLIENT_VERSION>>(value));
    return 1;
}

int lua_builder_game_reqs__door(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::door<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__emotion(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto value = static_cast<uint8_t>(lua->tointeger(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::emotion<BOT_CLIENT_VERSION>>(value));
    return 1;
}

int lua_builder_game_reqs__exit(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::exit<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__front_info(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::front_info<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__give_item(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto slot = static_cast<uint8_t>(lua->tointeger(1));
    const auto all  = lua->toboolean(2);
    lua_protocol::push_request(L, std::make_shared<game_reqs::give_item<BOT_CLIENT_VERSION>>(slot, all));
    return 1;
}

int lua_builder_game_reqs__give_money(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto money = static_cast<uint32_t>(lua->tointeger(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::give_money<BOT_CLIENT_VERSION>>(money));
    return 1;
}

int lua_builder_game_reqs__group(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto name = lua->tostring(1);
    lua_protocol::push_request(L, std::make_shared<game_reqs::group<BOT_CLIENT_VERSION>>(name));
    return 1;
}

int lua_builder_game_reqs__item_active(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto index = static_cast<uint8_t>(lua->tointeger(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::item_active<BOT_CLIENT_VERSION>>(index));
    return 1;
}

int lua_builder_game_reqs__item_drop(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto index = static_cast<uint8_t>(lua->tointeger(1));
    const auto all   = lua->toboolean(2);
    lua_protocol::push_request(L, std::make_shared<game_reqs::item_drop<BOT_CLIENT_VERSION>>(index, all));
    return 1;
}

int lua_builder_game_reqs__item_drop_money(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto chunk = static_cast<uint32_t>(lua->tointeger(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::item_drop_money<BOT_CLIENT_VERSION>>(chunk));
    return 1;
}

int lua_builder_game_reqs__item_inactive(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto parts = fb::model::enum_value::enum_parse<fb::model::enum_value::EQUIPMENT_PARTS>(lua->tostring(1));
    lua_protocol::push_request(L, std::make_shared<game_reqs::item_inactive<BOT_CLIENT_VERSION>>(parts));
    return 1;
}

int lua_builder_game_reqs__item_info(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto position = static_cast<uint16_t>(lua->tointeger(1));
    const auto slot     = static_cast<uint8_t>(lua->tointeger(2));
    lua_protocol::push_request(L, std::make_shared<game_reqs::item_info<BOT_CLIENT_VERSION>>(position, slot));
    return 1;
}

int lua_builder_game_reqs__item_throws(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto all   = lua->toboolean(1);
    const auto index = static_cast<uint8_t>(lua->tointeger(2));
    lua_protocol::push_request(L, std::make_shared<game_reqs::item_throws<BOT_CLIENT_VERSION>>(all, index));
    return 1;
}

int lua_builder_game_reqs__loot(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto boost = lua->toboolean(1);
    lua_protocol::push_request(L, std::make_shared<game_reqs::loot<BOT_CLIENT_VERSION>>(boost));
    return 1;
}

int lua_builder_game_reqs__map_update(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::map_update<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__map_world(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto value  = static_cast<uint16_t>(lua->tointeger(1));
    const auto before = static_cast<uint16_t>(lua->tointeger(2));
    const auto after  = static_cast<uint16_t>(lua->tointeger(3));
    lua_protocol::push_request(L, std::make_shared<game_reqs::map_world<BOT_CLIENT_VERSION>>(value, before, after));
    return 1;
}

int lua_builder_game_reqs__miss(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::miss<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__move(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto direction       = fb::model::enum_value::enum_parse<fb::model::enum_value::DIRECTION>(lua->tostring(1));
    const auto walk_queue_slot = static_cast<uint32_t>(lua->tointeger(2));
    fb::model::point<uint16_t> position;
    if (lua->is_table(3))
    {
        lua->rawgeti(3, 1);
        position.x = static_cast<uint16_t>(lua->tointeger(-1));
        lua->pop(1);
        lua->rawgeti(3, 2);
        position.y = static_cast<uint16_t>(lua->tointeger(-1));
        lua->pop(1);
    }
    else
    {
        position.x = static_cast<uint16_t>(lua->tointeger(3));
        position.y = static_cast<uint16_t>(lua->tointeger(4));
    }
    lua_protocol::push_request(
        L,
        std::make_shared<game_reqs::move<BOT_CLIENT_VERSION>>(direction, walk_queue_slot, position));
    return 1;
}

int lua_builder_game_reqs__move_blocked(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::move_blocked<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__pong(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto token          = static_cast<uint32_t>(lua->tointeger(1));
    const auto client_tick_ms = static_cast<uint32_t>(lua->tointeger(2));
    lua_protocol::push_request(L, std::make_shared<game_reqs::pong<BOT_CLIENT_VERSION>>(token, client_tick_ms));
    return 1;
}

int lua_builder_game_reqs__popup_input_submit(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto param0 = static_cast<uint8_t>(lua->tointeger(1));
    const auto text   = lua->tostring(2);
    lua_protocol::push_request(L, std::make_shared<game_reqs::popup_input_submit<BOT_CLIENT_VERSION>>(param0, text));
    return 1;
}

int lua_builder_game_reqs__post(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::post<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__screen_refresh(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::screen_refresh<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__self_info(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::self_info<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__spell_cast(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto type    = fb::model::enum_value::enum_parse<fb::model::enum_value::SPELL_TYPE>(lua->tostring(1));
    const auto slot    = static_cast<uint8_t>(lua->tointeger(2));
    const auto message = lua->tostring(3);
    const auto oid     = static_cast<uint32_t>(lua->tointeger(4));
    fb::model::point<uint16_t> position;
    if (lua->is_table(5))
    {
        lua->rawgeti(5, 1);
        position.x = static_cast<uint16_t>(lua->tointeger(-1));
        lua->pop(1);
        lua->rawgeti(5, 2);
        position.y = static_cast<uint16_t>(lua->tointeger(-1));
        lua->pop(1);
    }
    else
    {
        position.x = static_cast<uint16_t>(lua->tointeger(5));
        position.y = static_cast<uint16_t>(lua->tointeger(6));
    }
    lua_protocol::push_request(
        L,
        std::make_shared<game_reqs::spell_cast<BOT_CLIENT_VERSION>>(type, slot, message, oid, position));
    return 1;
}

int lua_builder_game_reqs__swap(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto type = fb::model::enum_value::enum_parse<fb::model::enum_value::SWAP_TYPE>(lua->tostring(1));
    const auto src  = static_cast<uint8_t>(lua->tointeger(2));
    const auto dst  = static_cast<uint8_t>(lua->tointeger(3));
    lua_protocol::push_request(L, std::make_shared<game_reqs::swap<BOT_CLIENT_VERSION>>(type, src, dst));
    return 1;
}

int lua_builder_game_reqs__unknown_54(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto token = static_cast<uint32_t>(lua->tointeger(1));
    const auto phase = static_cast<uint8_t>(lua->tointeger(2));
    lua_protocol::push_request(L, std::make_shared<game_reqs::unknown_54<BOT_CLIENT_VERSION>>(token, phase));
    return 1;
}

int lua_builder_game_reqs__update_option(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto option = fb::model::enum_value::enum_parse<fb::model::enum_value::OPTION>(lua->tostring(1));
    const auto ride   = lua->toboolean(2);
    lua_protocol::push_request(L, std::make_shared<game_reqs::update_option<BOT_CLIENT_VERSION>>(option, ride));
    return 1;
}

int lua_builder_game_reqs__user_list(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::user_list<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__web_map(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    lua_protocol::push_request(L, std::make_shared<game_reqs::web_map<BOT_CLIENT_VERSION>>());
    return 1;
}

int lua_builder_game_reqs__whisper(lua_State* L)
{
    auto lua = fb::lua::get(L);
    if (lua == nullptr)
        return luaL_error(L, "integration protocol requires active lua context");

    const auto name    = lua->tostring(1);
    const auto message = lua->tostring(2);
    lua_protocol::push_request(L, std::make_shared<game_reqs::whisper<BOT_CLIENT_VERSION>>(name, message));
    return 1;
}

void register_builders(lua_State* L)
{
    lua_newtable(L);
    lua_pushcfunction(L, lua_builder_game_reqs__attack);
    lua_setfield(L, -2, "attack");
    lua_pushcfunction(L, lua_builder_game_reqs__browser);
    lua_setfield(L, -2, "browser");
    lua_pushcfunction(L, lua_builder_game_reqs__bulletin);
    lua_setfield(L, -2, "bulletin");
    lua_pushcfunction(L, lua_builder_game_reqs__chat);
    lua_setfield(L, -2, "chat");
    lua_pushcfunction(L, lua_builder_game_reqs__click);
    lua_setfield(L, -2, "click");
    lua_pushcfunction(L, lua_builder_game_reqs__collection);
    lua_setfield(L, -2, "collection");
    lua_pushcfunction(L, lua_builder_game_reqs__direction);
    lua_setfield(L, -2, "direction");
    lua_pushcfunction(L, lua_builder_game_reqs__door);
    lua_setfield(L, -2, "door");
    lua_pushcfunction(L, lua_builder_game_reqs__emotion);
    lua_setfield(L, -2, "emotion");
    lua_pushcfunction(L, lua_builder_game_reqs__exit);
    lua_setfield(L, -2, "exit");
    lua_pushcfunction(L, lua_builder_game_reqs__front_info);
    lua_setfield(L, -2, "front_info");
    lua_pushcfunction(L, lua_builder_game_reqs__give_item);
    lua_setfield(L, -2, "give_item");
    lua_pushcfunction(L, lua_builder_game_reqs__give_money);
    lua_setfield(L, -2, "give_money");
    lua_pushcfunction(L, lua_builder_game_reqs__group);
    lua_setfield(L, -2, "group");
    lua_pushcfunction(L, lua_builder_game_reqs__item_active);
    lua_setfield(L, -2, "item_active");
    lua_pushcfunction(L, lua_builder_game_reqs__item_drop);
    lua_setfield(L, -2, "item_drop");
    lua_pushcfunction(L, lua_builder_game_reqs__item_drop_money);
    lua_setfield(L, -2, "item_drop_money");
    lua_pushcfunction(L, lua_builder_game_reqs__item_inactive);
    lua_setfield(L, -2, "item_inactive");
    lua_pushcfunction(L, lua_builder_game_reqs__item_info);
    lua_setfield(L, -2, "item_info");
    lua_pushcfunction(L, lua_builder_game_reqs__item_throws);
    lua_setfield(L, -2, "item_throws");
    lua_pushcfunction(L, lua_builder_game_reqs__loot);
    lua_setfield(L, -2, "loot");
    lua_pushcfunction(L, lua_builder_game_reqs__map_update);
    lua_setfield(L, -2, "map_update");
    lua_pushcfunction(L, lua_builder_game_reqs__map_world);
    lua_setfield(L, -2, "map_world");
    lua_pushcfunction(L, lua_builder_game_reqs__miss);
    lua_setfield(L, -2, "miss");
    lua_pushcfunction(L, lua_builder_game_reqs__move);
    lua_setfield(L, -2, "move");
    lua_pushcfunction(L, lua_builder_game_reqs__move_blocked);
    lua_setfield(L, -2, "move_blocked");
    lua_pushcfunction(L, lua_builder_game_reqs__pong);
    lua_setfield(L, -2, "pong");
    lua_pushcfunction(L, lua_builder_game_reqs__popup_input_submit);
    lua_setfield(L, -2, "popup_input_submit");
    lua_pushcfunction(L, lua_builder_game_reqs__post);
    lua_setfield(L, -2, "post");
    lua_pushcfunction(L, lua_builder_game_reqs__screen_refresh);
    lua_setfield(L, -2, "screen_refresh");
    lua_pushcfunction(L, lua_builder_game_reqs__self_info);
    lua_setfield(L, -2, "self_info");
    lua_pushcfunction(L, lua_builder_game_reqs__spell_cast);
    lua_setfield(L, -2, "spell_cast");
    lua_pushcfunction(L, lua_builder_game_reqs__swap);
    lua_setfield(L, -2, "swap");
    lua_pushcfunction(L, lua_builder_game_reqs__unknown_54);
    lua_setfield(L, -2, "unknown_54");
    lua_pushcfunction(L, lua_builder_game_reqs__update_option);
    lua_setfield(L, -2, "update_option");
    lua_pushcfunction(L, lua_builder_game_reqs__user_list);
    lua_setfield(L, -2, "user_list");
    lua_pushcfunction(L, lua_builder_game_reqs__web_map);
    lua_setfield(L, -2, "web_map");
    lua_pushcfunction(L, lua_builder_game_reqs__whisper);
    lua_setfield(L, -2, "whisper");
}

} // namespace fb::bot::integration::lua_protocol
