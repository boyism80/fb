-- spell: 인어장군잡기
-- 인어장군잡기 지속 효과

return {
    -- on_cast = function(me, you, sp)
    -- end,

    -- on_buff = function(me, sp)
    -- end,

    -- on_unbuff = function(me, sp)
    -- end,

    -- on_concast = function(me, sp)
    -- end
}
