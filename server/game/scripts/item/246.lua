-- item: 나비채
-- Catch logic runs in butterfly mob on_mob_damaged (lib/butterfly_catch.lua).

return {
    -- on_activated = function(me, item)
    -- end,

    -- on_deactivated = function(me, item)
    -- end,

    -- on_concast = function(me, item)
    -- end,
}
